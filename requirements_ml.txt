# Additional dependencies for the ml/ pipeline.
# Separate from requirements.txt so the existing Streamlit deployment is
# unaffected. Install with:
#   pip install -r requirements_ml.txt
imbalanced-learn>=0.11.0
